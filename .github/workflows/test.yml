name: Test Action

on:
  push:
    branches: [main, 'claude/**']
  pull_request:

jobs:
  test-action-syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate action.yml
        run: |
          # Verify action.yml is valid YAML
          python3 -c "
import yaml, sys
with open('action.yml') as f:
    data = yaml.safe_load(f)
required = ['name', 'description', 'inputs', 'outputs', 'runs']
for key in required:
    if key not in data:
        print(f'Missing key: {key}', file=sys.stderr)
        sys.exit(1)
print('action.yml valid')
"

      - name: Check evaluate step present
        run: |
          grep -q 'v1-evaluate' src/index.ts && echo 'evaluate step found'

      - name: Check verify step present
        run: |
          grep -q 'v1-verify-permit' src/index.ts && echo 'verify step found'

      - name: Check fail-closed logic
        run: |
          grep -q 'fail-closed' src/index.ts && echo 'fail-closed logic found'

      - name: Build and typecheck
        run: |
          npm install
          npm run typecheck
          npm run build
