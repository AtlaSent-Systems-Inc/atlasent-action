{
  "name": "atlasent-action",
  "version": "2.0.0",
  "description": "AtlaSent Policy Gate GitHub Action v2",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --license licenses.txt",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.0"
  }
}
